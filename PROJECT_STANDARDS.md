# Project Standards & AI Workflow

এই ফাইলটি প্রজেক্টের কোডিং স্ট্যান্ডার্ড এবং এআই (AI) অ্যাসিস্ট্যান্টের কাজের ধারা (Workflow) নির্ধারণ করে। এআই অ্যাসিস্ট্যান্টকে কোনো কাজ দেওয়া হলে সে সরাসরি কোড লেখা শুরু করবে না, বরং নিচের নিয়মগুলো মেনে চলবে।

## 🤖 AI Assistant Workflow (কাজের ধারা)

যেকোনো নতুন ফিচার তৈরি বা কোড মডিফাই করার আগে এআই অ্যাসিস্ট্যান্ট নিচের ৫টি ধাপ অনুসরণ করবে:

1. **প্ল্যান (Plan):** 
   - কাজটি কীভাবে করা হবে তার একটি পরিষ্কার রূপরেখা তৈরি করা।
   - কোন কোন ফাইলে পরিবর্তন আসবে বা নতুন কী কী ফাইল তৈরি হবে তার তালিকা দেওয়া।

2. **প্রভাব (Impact Analysis):** 
   - এই পরিবর্তনের ফলে প্রজেক্টের অন্য কোনো ফাইলে বা ফিচারে কোনো সমস্যা (Bug) হওয়ার সম্ভাবনা আছে কিনা তা যাচাই করা।

3. **সুবিধা-অসুবিধা (Pros & Cons):** 
   - প্রস্তাবিত সমাধানের ভালো দিক এবং খারাপ দিকগুলো তুলে ধরা।

4. **আধুনিক পদ্ধতি (Modern Best Practices):** 
   - বর্তমান সময়ের আধুনিক ও মডার্ন সিস্টেম অনুযায়ী কাজটি কীভাবে করা উচিত তার সঠিক পরামর্শ দেওয়া।

5. **অনুমতির অপেক্ষা (Wait for Approval):** 
   - উপরের ৪টি ধাপ সম্পন্ন করে এআই ব্যবহারকারীর অনুমতির অপেক্ষা করবে। ব্যবহারকারী "হ্যাঁ", "কাজ শুরু করো" বা "Proceed" বললে তবেই এআই কোড লেখা বা পরিবর্তন করা শুরু করবে।

---

## 💻 Coding Standards (কোডিং নিয়মাবলি)

প্রজেক্টের কোডবেস পরিষ্কার, আধুনিক এবং মেইনটেইনেবল রাখার জন্য নিচের নিয়মগুলো মেনে চলতে হবে:

### ১. আর্কিটেকচার ও ফোল্ডার স্ট্রাকচার
- **Separation of Concerns:** UI কম্পোনেন্ট এবং বিজনেস লজিক আলাদা রাখতে হবে।
- **Custom Hooks:** কম্পোনেন্টের ভেতরের বড় লজিক বা স্টেট ম্যানেজমেন্টগুলো `hooks/` ফোল্ডারে কাস্টম হুক (যেমন: `useSidebar.ts`, `useTranscription.ts`) হিসেবে আলাদা করতে হবে।
- **Constants:** যেকোনো ফিক্সড টেক্সট, এআই প্রম্পট বা কনফিগারেশন `constants/` ফোল্ডারে রাখতে হবে।

### ২. রিঅ্যাক্ট (React) ও টাইপস্ক্রিপ্ট (TypeScript)
- সব সময় Functional Components ব্যবহার করতে হবে।
- `any` টাইপ যথাসম্ভব এড়িয়ে চলতে হবে এবং সঠিক TypeScript Interface বা Type ডিফাইন করতে হবে।
- কম্পোনেন্টগুলো ছোট এবং রিইউজেবল (Reusable) রাখতে হবে। একটি কম্পোনেন্ট খুব বড় হয়ে গেলে তা ভেঙে ছোট ছোট কম্পোনেন্টে ভাগ করতে হবে।

### ৩. স্টাইলিং (Styling)
- স্টাইলিংয়ের জন্য শুধুমাত্র **Tailwind CSS** ব্যবহার করতে হবে।
- ইনলাইন স্টাইল (`style={{...}}`) এড়িয়ে চলতে হবে।

### ৪. পারফরম্যান্স (Performance)
- অপ্রয়োজনীয় রি-রেন্ডার (Re-render) রোধ করতে হবে।
- বড় লিস্ট রেন্ডার করার সময় `key` প্রপ সঠিকভাবে ব্যবহার করতে হবে।

---
**নোট:** এআই অ্যাসিস্ট্যান্ট এই ফাইলটি সবসময় মনে রাখবে এবং এর নির্দেশিকা অনুযায়ী কাজ করবে।
